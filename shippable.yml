# Language setting
language: java
services:
  - elasticsearch

# Version number
jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
  - oraclejdk8

build:
  ci:
    - java -version
  pre_ci_boot:
    image_name: drydock/u12javpls
    image_tag: prod
    pull: true    
